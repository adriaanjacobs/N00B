#pragma once

#define TAG_WIDTH                   (${TAG_WIDTH})
#define NOOB_TOPTAG_START           (${NOOB_TOPTAG_START})
static_assert(NOOB_TOPTAG_START + TAG_WIDTH <= 64);
#define NOOB_MIN_RADIX              (${NOOB_MIN_RADIX})
#define NON_NOOB_MIN_RADIX          (${NON_NOOB_MIN_RADIX})
#define NOOB_IGNORE_ERRORS          (${NOOB_IGNORE_ERRORS})
#define NOOB_TAG_POINTERS           (${NOOB_TAG_POINTERS})
#define NOOB_MAX_ADDR               (${NOOB_MAX_ADDR})

#define ARITH_LEEWAY_WIDTH          (${ARITH_LEEWAY_WIDTH})
static_assert(ARITH_LEEWAY_WIDTH == 0, "we don't need to permit additional arithmetic");
#define ARITH_LEEWAY_OCCUPIED_BITS  (${ARITH_LEEWAY_OCCUPIED_BITS})
static_assert(ARITH_LEEWAY_OCCUPIED_BITS < (1U << ARITH_LEEWAY_WIDTH));
#define NOOB_STACK_SIZE             (${NOOB_STACK_SIZE})

#define CHECK_DEREFERENCE_SITES     ${CHECK_DEREFERENCE_SITES}
#define CHECK_ESCAPE_SITES          ${CHECK_ESCAPE_SITES}
#define CHECK_POINTER_ARITHMETIC    ${CHECK_POINTER_ARITHMETIC}
#define CHECK_POINTER_DEREFERENCES  ${CHECK_POINTER_DEREFERENCES}
#define USE_BRANCHING_CHECKS        ${USE_BRANCHING_CHECKS}
#define SOUND_POINTER_DETECTION     ${SOUND_POINTER_DETECTION}

#define REPLACE_STACK_ALLOCS        ${REPLACE_STACK_ALLOCS}
#define REMAP_GLOBALS               ${REMAP_GLOBALS}

#define EMIT_RUNTIME_CALLS          ${EMIT_RUNTIME_CALLS}
#define TRACK_ARITH_STATS           ${TRACK_ARITH_STATS}
static_assert(TRACK_ARITH_STATS ? EMIT_RUNTIME_CALLS : 1, "EMIT_RUNTIME_CALLS must be enabled to use TRACK_ARITH_STATS");

#define EMULATE_LOWFAT              ${EMULATE_LOWFAT}
#define PAD_BY_ONE_BYTE             ${PAD_BY_ONE_BYTE}
